# Editor Architecture

> **本文件负责**: ProseMirror / CodeMirror 编辑器栈、markdownIO、语法实时转换、NodeView/Decoration、mermaid、shiki 与跨模式同步。
>
> **何时阅读**: 改 schema、markdownIO、ProseMirror 插件链、NodeView/Decoration、代码高亮、源代码模式、快捷键或新增 markdown 语法（见下方「新增语法支持 checklist」）时。
>
> **先记住**:
> - `EditorInner.vue` 的 `allPlugins` 顺序是行为契约，改动需同步本文件。
> - mermaid / TOC 走 Decoration.widget，不走 NodeView；不要 dispatch setMeta 触发 mermaid widget rebuild。
> - shiki dark/light 翻面走 CSS；换主题 / 懒加载 lang 才触发受控 rebuild。
> - syntaxAutoFormat 的 apply 直接改框架传入的 `tr`，不要自己 dispatch。
> - markdownIO 双向改动必须补/跑 round-trip。
>
> **相关文件**: [架构入口](../ARCHITECTURE.md) / [查找替换](./find-replace.md) / [导出](./export.md)


## 禁令速查

- 不要 `dispatch setMeta` 触发 mermaid / shiki 重建；mermaid 直接写 widget DOM，shiki darkMode 纯 CSS。
- 不要用 `getLanguage()` 探活 shiki miss；用 `getLoadedLanguages()`。
- 不要把脚注编号写回 `attrs.label`。
- 快捷键不要在 `EditorInner.vue` 硬编码，走 `editor/shortcuts/bindings.ts`。
- `[TOC]` 回写 toMarkdown 不要用 text 节点。
- 不要清理 `getTokensCached` 这层 LRU。

## ProseMirror 插件链

按 `EditorInner.vue` 里 `allPlugins` 数组顺序:

| 插件 | 用途 |
|------|------|
| codeBlockLangSuggestPlugin | ``` 语言建议下拉:paragraph 中键入 ``` + 语言前缀时浮出模糊匹配下拉,上下键导航 + Enter 提交 + Escape 关闭(见设计要点) |
| keymap(Backspace/Delete → headingToParagraph) | 标题退格/删除转段落 (不降级 h2→h1) |
| keymap(Mod-z/y/Shift-z) | 撤销/重做 |
| keymap(Enter → dollarEnterCmd) | `$$`+Enter 进块级公式编辑态 |
| keymap(baseKeymap) | 接管基础键 |
| dropCursor / gapCursor | 拖放光标 + 跨非文本节点光标 |
| history | 撤销/重做栈 |
| tabIndent | Tab 缩进/反缩进;代码/段落插4空格;非列表 Shift-Tab 消费 |
| imageKeymapPlugin | atom 节点删除保护 (Backspace/Delete 紧贴先选中不直接删) |
| imageUploadPlugin | paste/drop 拦截 → 落盘 → 插入 image 节点。OS 拖入只接 image/*(文本返回 false);文件树拖入(自定义 MIME `application/x-velo-tree-path`,仅文件) .md 打开 / 图片落盘插图;**目录走独立 MIME `application/x-velo-tree-dir-path` 且不写 text/plain**,编辑器侧不识别 → 目录自然无法拖入编辑器(防止误把路径串当文本插)。共享 `image/treeDrop.ts` |
| markdownPastePlugin | text/plain 粘贴走 fromMarkdown,绕开 ProseMirror 默认 plain-text fallback 的 normalizeSiblings 错误合并(见设计要点) |
| linkClickPlugin + linkEditEscapeKeymap | 链接单击进源码编辑 / Cmd 跳转 / Escape 还原 |
| imageEditPlugin + imageEditEscapeKeymap | 图片选中后右上角 code-xml 按钮进源码编辑 session(replaceWith 纯文本 → 光标移出 commit / Escape 还原,见设计要点) |
| syntaxAutoFormatPlugin | dirty-range 局部扫,registry 驱动 (见设计要点) |
| markSourceEditPlugin | mark 源码编辑 session(Obsidian Live Preview 风格):光标进入 **bold** 等 mark → 整段换源码字符可编辑,移出 commit 还原 (见设计要点) |
| codeHighlightPlugin | shiki dual-theme 代码高亮 + header 标题栏 widget(折叠 chevron + 语言选择 + wrap toggle + 复制,见设计要点) |
| codeWrapPlugin | code_block 自动换行 toggle:per-block `Set<number>` 跟踪 wrap 状态,`Decoration.node` 挂 `data-velo-wrap="true"` 驱动 SCSS `pre-wrap`,header widget key 含 wrap 状态(见设计要点) |
| codeLineNumberPlugin | WYSIWYG 代码块行号 widget(可选开关,默认关闭;mermaid 跳过;wrap 模式下动态行高同步,见设计要点) |
| imageInlineViewPlugin | image NodeView (Tauri asset:// 代理 + 选中态常驻 code-xml 编辑按钮) |
| hrNodeViewPlugin | hr NodeView:atom block 包裹在 `<div class="velo-hr">`,selectNode/deselectNode 切换 `.selected` 画 outline |
| frontmatterNodeViewPlugin | YAML frontmatter NodeView(Typora 风格 styled code block + "Front Matter" 标题栏,内容可直接编辑) |
| mathEditPlugin | math_inline 双模 NodeView(Obsidian/Typora 风格:光标外渲染 / 光标内 source 文本,见设计要点) + math_block 编辑壳 NodeView(KaTeX preview + textarea,见设计要点) |
| mermaidDecoration | 扫 `code_block { language: 'mermaid' }` 渲染 SVG / 编辑态切换 widget (见设计要点) |
| taskListPlugin | `- [ ]` / `- [x]` checkbox NodeView |
| footnoteEditPlugin | 脚注 NodeView + 位置收集 |
| tocDecoration | `[TOC]` 目录 Decoration.widget (嵌套标题列表 + 点击跳转) |
| foldDecoration | 块级折叠(heading / list_item / code_block):toggle chevron + placeholder + fold 区段 display:none,stable key 持久化 |
| findHighlight | 查找替换高亮 |
| focusModePlugin | 专注模式:光标所在顶层块挂 `.velo-focus-active` class,CSS 层降其余块透明度 |
| typewriterModePlugin | 打字机模式:光标锁视口中线,`handleScrollToSelection` 抑制 PM 最小滚动 + rAF 居中(见设计要点) |
| `buildShortcutKeymap`(editor/shortcuts)| declarative registry 输出的快捷键 keymap,统一在 `bindings.ts` 注册 |
| inputRules | ellipsis/emDash 纯文本快速路径 (其余语法走 syntaxAutoFormat) |

**markdown 解析**走 `editor/markdownIO.ts` 的 unified pipeline (remark-parse + remark-gfm + 公式围栏守卫 + remark-math + preserveEmptyLine + remark-frontmatter)。`fromMarkdown(md)` → EditorState,`toMarkdown(doc)` 回写。**键入触发**走 syntaxAutoFormat,不走 unified。

---

---

## 设计要点

- **mermaid 走 Decoration.widget 不走 NodeView**: atom NodeView 的 outer dom `innerHTML` 变更会被 ProseMirror DOMObserver 当外部突变 → 全量 remount + 每字符 loader 闪烁;widget 的 `ignoreMutation` 默认忽略非 selection 突变。当前 `mermaid` 节点已废弃,```` ```mermaid ```` 改走 `code_block { language: 'mermaid' }`,由 `MermaidDecoration` 扫 code_block 渲染 SVG widget + 自管切换/删除/关闭按钮(默认 pre 隐藏,SVG `side:-1` 渲染在其前;点击 SVG 派发 setMeta 翻转 editNodePos,展开时 `queueMicrotask` 把光标放进 pre)。**坑**: plugin promise resolve 后不要 dispatch setMeta rebuild decorations,直接在 widget dom 上写 svg,否则 `WidgetType.eq` 失效 → 死循环;主题切换同理走 window listener 自改 dom + `spec.destroy` removeEventListener 防泄漏。PM 搜索命中隐藏的 mermaid 源码时,必须先走幂等展开 helper 再做 `TextSelection` / `coordsAtPos` / 滚动;不要直接 blind toggle,否则已展开块会被收起,或 Mermaid 自带 pending focus 抢走搜索选区
- **mermaid + codeHighlight 双 plugin 分工**: codeHighlight 对普通 code_block 出 header 标题栏(折叠 chevron + 语言选择 + 复制)+ 语法高亮;mermaid 的 header **联动展开态** —— 收起态(显示 SVG / 源码隐藏,`editNodeSet` 不含本 pos)跳过 header,避免与 MermaidDecoration 的 SVG + 切换/删除 toolbar 叠双层;展开态(源码可见)仍保留 header(语言选择 + 复制可用),但 **fold chevron 隐藏**(`hideFoldBtn = mermaidExpanded`)—— 避免手闲点 fold 误触发 `FoldDecoration` 折叠 code_block → `isMermaidFolded` 把 SVG 也吞掉;mermaid 的"收"由 mermaid toolbar toggle 承担,header 提供 fold 入口只会有害。`renderHeader = !isMermaid || editNodeSet.has(pos + 1)`。与 `CodeLineNumberWidget`("mermaid code_block → 无行号")同范式。跳 header / 藏 chevron 都只是跳过 UI 容器,token 着色旁路(见下一条)始终 fall through
- **mermaid 语法高亮旁路 shiki**: shiki bundled mermaid grammar 薄(只输出按行纯文本 token),`codeHighlight` 对 `lang==='mermaid'` 走自写 `tokenizeMermaid`(6 类:keyword/direction/shape/edge/label/comment),颜色从当前代码块主题按 TextMate scope 提取 hex 写进 `--shiki-light/dark` 局部变量(同 shiki token 形态);主题切换/dark 切换两条路径同 shiki
- **shiki dual-theme 代码高亮**: `codeToTokensWithThemes` 返回 token 级双色,每 token inline `--shiki-light/dark` 变量,SCSS 按 `html.dark` 选。**darkMode toggle 纯 CSS 切色(零重渲);换主题(换 hex)才 rebuild**,由 App.vue watch 触发。首屏零闪烁:App.vue `codeBlockReady` 守门 PM mount;`state.init` 同步拿 cached highlighter
- **shiki 预扫 + 懒加载 lang**: 启动只装 doc 实际 lang ∪ 5 项 BASELINE(js/ts/py/bash/json);运行时 miss 用 `hl.getLoadedLanguages()` 探活(不能用 `getLanguage()`,miss 时 throw),异步追加 grammar 后**不直接 rebuild highlighter**,经 plugin 端 `setDecorationRebuildCallback` 钩子让 plugin 自己 rAF 节流 rebuild decorations(见下方"shiki 两条正交路径")。**首次 miss 那帧无 token 是有意为之的"先骨架后着色"**。`LANG_OPTIONS` 可能含非 shiki 合法 id(如 `react` — shiki 只有 `jsx`/`tsx`),`resolveShikiLang` 统一映射(react→jsx,语义等价:React 代码用的就是 JSX 语法)后传 shiki;`getHighlighter` 再过滤掉不在 `bundledLanguages` 里的 id,防 `createHighlighter` 抛 `ShikiError`(App.vue `.catch` 兜底虽不白屏,但 `cachedHighlighter` 永远 null → **整篇代码块高亮全降级**)
- **语法实时转换走 appendTransaction + dirty-range**(不走 InputRule 末尾匹配): `syntaxAutoFormat.ts` 从 `tr.mapping.maps` 提 dirty range → textblock 段首检测 + inline 正则扫描,黑名单(code_block/html_block/math_block)、code mark、link / image edit session 框架统一过滤。新增语法 = 写一个文件 + `syntax/index.ts` 注册一行。**坑**: block detector pattern 带 `^` 不带 `g`;inline 带带 `g` 不带 `^/$`;inline 扫描前 atom 用 NBSP 占位防穿透;语法 apply 直接改框架传入的 `tr`,不要自己 dispatch。**inline 扫描的 blockText 不能用 `doc.textBetween`**:它对"有 content 的非 text inline 节点"(footnote_reference `content:'text*'` / math_inline `content:'text*'`)会递进取 text content,输出的字符数 < 节点占的 doc 位置数(差 open+close tag 开销),match.index 映射回 doc 位置偏前 → replaceRangeWith 删错位置 / 删进节点边界。框架用自写的 `buildBlockText` 替代:text 节点追加 text content;inline 非 text 节点用 `\u0000` 占位(长度 = nodeSize,不递进 content);block 容器(doc/paragraph)下钻。黑名单检查从 `includes(' ')` 改成 `includes('\u0000')`
- **公式围栏守卫在 remark-math 前做文本预处理**: Velo 约定块级公式开头行必须只有 `$$`,同一行 `$$...$$` 继续按行内公式解析。`remark-math` 默认接受 `$$meta` 作为 flow math 开头,且 EOF 也能闭合,用户少写行尾 `$$` 时会把后续段落吞成一个 `math_block`;因此 pipeline 在 `remark-math` 前把“行首 `$$` 后有正文但本行没有闭合 `$$`”转义成普通文本。导出 HTML 复用同一守卫,避免编辑器与导出语义分叉
- **NodeView 隔离**: `ignoreMutation()` + `stopPropagation` 隔离 ProseMirror。**坑**:PM 的 `view.dom` 是 contentEditable 容器,所有 selection 统一由 PM 管理;在 NodeView 里嵌套 `contentEditable=true` 的子元素(sup / labelSpan)拿不到独立 focus —— 浏览器把 focus 给了子元素但 PM 的 selection 仍停在外层,Backspace/Delete 按 PM 的 selection(子元素外)处理 → "删错位置"。需要"光标进入、逐字符编辑"的 inline 节点(如 `footnote_reference` / `math_inline`)不要走 atom + contentEditable,改成 `content: 'text*'` 的非 atom 节点 + `contentDOM = dom`,让 PM 直接接管 text 编辑(selection 自然进入)。`footnote_reference` 的 label 作为 text content 而非 `attrs.label`,`math_inline` 的 source 同理(schema 早就 `content: 'text*'`,v0.5.9 起去 atom,见"math_inline 双模"),markdownIO 双向适配。`footnote_definition` 的 label 同样拆成 `footnote_label` block 节点(`content: 'text*'`,作为 `footnote_definition` 的强制首子)而非 `attrs.label`,NodeView 不再自管 `labelSpan` 的 input/keydown:`.footnote-label` 曾是 NodeView 自造、不在 contentDOM 子树内的元素,PM 看不到 → 点击 label 时 PM 把 selection 推进到描述段 `<dd>` 前,Backspace/Delete 删错位置。改后与 `footnote_reference` 完全同范式,PM 接管 label 文本,光标自然进入 `<dt>` 内编辑。`FootnoteNodeViews.ts` 的 `currentLabel` 从 `node.firstChild.textContent` 读 label,`defs` 映射同步从 `firstChild.textContent` 算;`computeNumbering` 一处统一两份来源
- **NodeView 必须实现 `stopEvent`(math_block 踩坑)**:`isolateInputFromProseMirror` 的 `stopPropagation` 只能拦"事件从 textarea 冒泡"这一条路径,PM 的 `eventBelongsToView` 还会从 `event.target` 沿祖先链对每个有 `pmViewDesc` 的节点问 `stopEvent` —— 这是 PM 隔离 NodeView 子元素(嵌套 textarea / input)的**主要**机制。math_block 编辑态挂 textarea + preview,`stopEvent` 不实现 → 用户敲字符时 PM 走默认 `tr.insertText(text, from, to)`,math_block 当 NodeSelection 被替换 → "math 节点消失 / 输入框消失 / 光标消失"(`$$`+Enter 那条路径尤其明显,atom + NodeSelection 选中是默认 selection 形态)。**两道闸都要有**:inner 一侧 `isolateInputFromProseMirror` `stopPropagation`(目标在 textarea 时的快速路径)+ outer 一侧 NodeView `stopEvent` 返回 `true` 对 `beforeinput / input / keydown / paste / composition*` 等输入事件族,两闸互不依赖,任一就够拦住 PM。math_inline 已去 atom(见下面"math_inline 双模"设计要点),不再挂自管 input/textarea,这条 stopEvent 机制对 inline 不适用
- **图片源码编辑走 linkClick 同款 session 范式(非 math_block textarea 浮层)**: image 是 atom inline 节点,渲染 `<img>` 后无法改 src/alt/title。选中(NodeSelection)→ 右上角常驻 code-xml 按钮(`widgetIcons.ts`,CSS 按 `.selected` 显隐 → 点按钮 `triggerImageEdit` 把 image 节点 `replaceWith` 成 `![alt](src "title")` 纯文本,默认选中 `()` 内的 src(改链接是最高频操作,无 title 时 src 正是 `()` 内全部);编辑期间图片不消失 —— Decoration.widget(`side:1`,渲染在源码文本下方)持图片预览,`key` 含当前源码文本 → 文本变 → 重建 widget → 实时跟随 src,源码残缺(不匹配 `parseImageSource`)→ 不挂 widget(图片隐藏,符合语法糖残缺不渲染)。用户像编辑普通文本一样改源码,`createImageEditPlugin` 的 session state(PluginKey 持有,生命周期跟 EditorState 一致)随 `tr.mapping` 平移 editFrom/editTo;光标移出范围 → `apply` 标 `pendingCommit` → `view.update` 触发 commit:`parseImageSource` 合法 → 重建 image 节点(NodeSelection 选中),残缺(删了 `!`/`(`)→ 保留为纯文本(Obsidian 降级),空值 → 删除;Escape → 重建点击前的原 image 节点(放弃编辑)。**与 linkClick 的关键差异**:linkClick 的 Escape 还原成 `[text](url)` 纯文本后靠 `syntaxAutoFormat` 重新转成渲染态 link mark;**image 没有实时键入转换**(syntax registry 无 image),纯文本 `![alt](src)` 不会自动变回 image,故 Escape 直接重建 image 节点而非插文本。session 状态机骨架(apply mapping + pendingCommit + view.update commit + Escape keymap)完全复用 linkClick。parse/serialize 纯函数在 `image/imageSource.ts`;imageNodeView 不持有编辑态 —— 替换成纯文本后本 NodeView 销毁,编辑期视觉层全靠预览 widget,commit 重建 image 后新 NodeView 创建、按钮重新出现。**proxyDomURL 复用**: image NodeView 与 edit 预览 widget 同一份 `resolveImageSrc`(EditorInner.vue),保证渲染态与编辑态预览解析一致。**坑(进编辑态不误判 dirty)**: trigger 事务(image→源码文本)挂 `SKIP_CONTENT_EMIT` meta(`editor/transactionMeta.ts`,useProseMirror 的 dispatchTransaction 见此 meta 跳过 onChange 内容回写)—— 这是瞬时视图切换不是内容编辑;若照常回写,toMarkdown 会把纯文本里的 `![...](` 转义成 `\![...](`,与渲染态 image 序列化结果不同,触发 `content !== lastSavedContent` → 展开源码期间标题栏常驻 "•"。commit / Escape **不挂**此 meta —— 它们需要回写把 content 重新同步到重建后的 image 形态(否则 content 停在编辑期间的转义文本上,Ctrl+S 写盘会写出错误的转义串)。**坑**: imageEdit session 必须进 `syntaxAutoFormat` 的退避列表(与 linkClick 并列,`getActiveEditRange` 同时读两个 session),否则无 title 源码 `![alt](src)` 内层 `[alt](src)` 命中 link inline 正则,光标还在 session 内就被即时转成 link mark,`!` 成孤儿(有 title 时因 url 段含空格不命中正则,侥幸不触发,故 bug 只在无 title 时暴露)。**坑(src 含括号不转义)**: 本地文件路径常含括号(如 `(null).png`)。`serializeImageSource` 不转义 src 中的 `(` `)` —— `parseImageSource` 正则 `[^']*?` 允许括号,靠 `\)\s*$` 锚定最后一个 `)` 为语法收尾。`toMarkdown` 同理:自定义 remark-stringify image handler 覆盖默认 `safe()`,不转义 URL 中的括号(CommonMark 允许 balanced 括号出现在 link destination,remark-parse 能正确解析)。URL 含空格时仍用 `<>` 包裹。`AssetPanel.vue` 的 `scanMarkdownImages` 正则同步改为 `(?:[^()\s]|\([^()]*\))*` 支持 balanced 括号
- **图片段落尾随空行(隐藏 PM trailing break)**: image 是 inline atom 但 NodeView `display:block`,当图片是段落末子时,PM 的 `addTextblockHacks`(prosemirror-view `viewdesc.ts`)规则"末子非 `TextViewDesc` 即补 `<br class="ProseMirror-trailingBreak">`"命中,在图片下方撑出一条空行。**Chrome/Safari/WebView2 上还会额外补一个零宽 `<img class="ProseMirror-separator">`**(因末子 `contentEditable == "false"`,本 image NodeView span 即是),故 DOM 形如 `[image-span][separator][trailingBreak]` —— 相邻兄弟选择器须同时覆盖"span 直连 br"与"span 经 separator 连 br"两条路径(Firefox 不加 separator,只走第一条)。SCSS `.velo-image-inline + .ProseMirror-trailingBreak, .velo-image-inline + .ProseMirror-separator + .ProseMirror-trailingBreak { display:none }` 仅隐藏紧随图片之后的那条;空段自己的 trailing break(高度/光标来源)不动。separator 零宽不撑行,无需处理。PM 内部仍按 `matchesHack` 复用此 `<br>`,仅视觉隐藏,无 PM 层副作用 —— 不要删这条 SCSS,否则图片下方空行回归
- **选中图片时键入不生效(NodeSelection inert)**: image 是 `contenteditable=false` 的 inline atom,NodeSelection 的 focus 端在 Chrome/WebView2 上被渲染到下一个可编辑文本(图片下方段落首字符)—— 选区范围整段落在不可编辑内容上,浏览器把可视光标"借位"到最近可编辑位置(纯渲染产物,PM state 仍是 NodeSelection,`.selected` 蓝框 / 编辑按钮依故可见),用户键入会误 insert 到那里。两道闸挡住(都在 `imageEditPlugin.props`):`handleKeyDown` 吞可打印字符(`key.length===1` 且无 ctrl/meta/alt → 返回 true,PM `preventDefault` → 浏览器不发 beforeinput → 不 insert,主路径无闪烁);`handleTextInput` 兜底返回 true(覆盖 keypress 路径 / 浏览器已 insert 触发 readDOMChange —— 后者走 start-of-operation selection,`domchange.ts:9` 注释,此时 `state.selection` 仍是 NodeSelection,吞掉 dispatch 后 flush `updateState` 把 DOM 多余文本回滚)。Backspace/Delete/方向键/Enter/Escape(`key.length>1`)不吞,PM 默认删图/移动;ctrl/meta/alt 组合不吞,快捷键照常。**视觉光标**:SCSS `.velo-editor:has(.velo-image-inline.selected) { caret-color: transparent }` 选中态隐藏整编辑器文本光标(`:has()` WebView2 原生支持);取消选中 `.selected` 移除后自动恢复。源码编辑态 image 已替换成文本(无 `.velo-image-inline.selected`),光标正常显示
- **YAML / TOML Front Matter 走 NodeView + content:'text*'（Typora 风格 styled code block）**: `remark-frontmatter(['yaml','toml'])` 把文档首部 `---`(yaml) / `+++(toml)` 包裹的块解析为对应 mdast 节点,markdownIO 双向转成 PM `frontmatter` 节点,**并通过 `lang` 属性记录种类**('yaml'/'toml',决定序列化分隔符 + shiki grammar)。schema `content:'text*'` + `code:true` + `marks:''`（同 code_block 隔离语义）;**不设 group** — 不属于 block/inline,只通过 `doc` content `'frontmatter? block+'` 出现,限定只能出现在文档首部且最多一个。NodeView(`FrontmatterNodeView.ts`)渲染 `<div class="velo-frontmatter" data-lang>` + header 标题栏(左侧折叠 chevron + 中间 "Front Matter" 标题 + **右侧格式 chip**("YAML"/"TOML" 药丸按钮)) + `<pre><code>` contentDOM（PM 接管文本编辑,光标自然进入）。`stopEvent` 仅隔离 header 内事件(防点 header 时光标跳到 contentDOM);`ignoreMutation` 仅忽略 header 的 mutation(contentDOM 内文本突变由 PM DOMObserver 正常处理)。**格式切换**:header 右侧朴素文字按钮("YAML"/"TOML",继承标题栏字号,虚线下划线暗示可点击),点击 dispatch `setNodeMarkup` 切换 `lang`(yaml ↔ toml)——序列化 fence + shiki grammar 同步跟随;文案 + wrapper `data-lang` 由 `update()` 跟住(PM 驱动,不重建 NodeView)。刻意不用强调色 / 边框,避免抢内容区注意力。**语法高亮**:frontmatter 节点按 `lang` 走对应 shiki grammar 着色(yaml / toml 各自独立 grammar),由 `codeHighlightPlugin` 的 `buildDecorations` 在 doc walk 中读 `node.attrs.lang` 走 `getTokensCached` + dual-theme `--shiki-light/dark` inline decoration 路径(与 code_block 同机制,只是不提供 header widget / 语言选择,因 NodeView 自管标题栏)。前端预装:markdownIO 的 `extractLangsFromDoc` 在 mdast 见 `yaml`/`toml` 节点时把对应 id 加入 seed,App.vue 启动期即装 grammar → 首屏直接出 token(无先黑后色闪烁)。pre/code 基础色由 `_editor-frontmatter.scss` 改用 `var(--shiki-light/dark)`,token span 由 `_editor-code.scss` 的 `.velo-editor pre span` 规则接管(frontmatter `<pre>` 在 `.velo-editor` 内,无需新规则)。**序列化**:`toMarkdown` 按 `lang` 把节点转回对应种类 mdast 节点(`{type:lang,value}`),remark-frontmatter handler 按种类包裹对应 fence(`---`/`+++) 输出。导入/出一致:toml 走 `+++` round-trip,不降级为 `---`。导出 HTML 剥离 frontmatter(`htmlRenderer.ts` 的 `case 'yaml': case 'toml': return ''`,元数据非内容)。**文件只有 frontmatter 无正文时** `fromMarkdown` 补一个空 paragraph 满足 `doc` content `block+` 要求。**实时键入**:`syntax/block/frontmatter.ts` 的 `frontmatterEnterCommand` 插在 Enter keymap 链中 `hrEnterCommand` **之前**(文档首段 `---`/`+++`+Enter → frontmatter,非首段 fall through 到 hr);`frontmatterSyntax` 注册在 `hrSyntax` **之前**(`--- `/`+++ ` 空格触发,同优先级拦截)。**fence 检测(frontmatterFenceKind)与 parser 严格对齐**:只接受恰好 3 根 `-`/`+`(前导空格 ≤3,尾部可跟空白),不接受 `*`/`_`(那些归 hr)和 4+ 根(避免 `++++` 误转后序列化回 `+++` 的 round-trip 丢失)。两者均校验:段落是 doc 第一个子节点 + doc 中无已有 frontmatter,条件不满足时 return false 让 hr 接管
- **空文档 canonical 形式必须是 '',不能是 '\n\n\n'**:`toMarkdown` 的尾部空行补偿(按尾部连续空段数补 2K+1 个 \n)是为「真实内容之后的预留空行」设计的(`b\n\n\n` 严格 idempotent),但它也会命中「整篇只有一个 schema 强制占位 paragraph」的空文档,把它序列化成 `'\n\n\n'`。这个串在源码模式下被 CM6 原样渲染成 **4 行空行**,WYSIWYG 经 `fromMarkdown` 却只折成 **1 个空段落(1 行)** —— 两模式对同一内容给出不同行数,用户感知「新建文档自动多出 4 行」。因此补偿前必须判 `k === doc.childCount`(整篇全是空段)→ 直接 `return ''`。'' 经 `fromMarkdown` 仍还原为空文档(round-trip 闭合),且源码模式 / WYSIWYG 都是 1 行。下游 `documentStore` 的 `isPristineBlank` / `createTab` 据此只认 '' 为空文档基线,**不必**再为「挂载即 dirty」而预置 `'\n\n\n'` 基线(EditorInner emit 的 1 空段 → `toMarkdown` → '' 与基线一致,不会误 dirty)
- **hr 走 block+atom + NodeView + imageKeymap 三件套(可选中 + Backspace/Delete 删除)**:hr 是 void block 节点,默认不可选、光标落到 hr 行会"消失"(无文本位置)。改法完全复用 image/math_block 范式 —— schema 加 `selectable: true, atom: true, defining: true`(与 math_block 同形态),`nodes/HrNodeView.ts` 提供 `<div class="velo-hr">` 包裹的 NodeView(selectNode/deselectNode 切换 `.selected`),`image/imageKeymap.ts` 的 `ATOM_TYPES` 纳入 `'hr'`(光标紧贴 hr 时 Backspace/Delete 先选中再二次删除)。**与 image 的关键差异**:image 是 inline atom(光标紧贴时 `nodeBefore/nodeAfter` 直接命中),hr 是 block atom(nodeSize 1,与 math_block 同)—— 光标落到 hr 后的段落起点时 `nodeBefore` 为 undefined(深度 1 内),所以 hr 的"选中"主要靠点击产生 NodeSelection(同 math_block),而非 image 那样的相邻键选中;`ATOM_TYPES` 纳入 hr 仍必要,覆盖 Delete 键从 hr 前一段落末尾删除的场景(`nodeAfter` 命中 hr)。**NodeView 比 image 简单**:hr 无 src/alt/title 属性,无需编辑按钮 / 源码编辑 session,dom 就是一个 `<div class="velo-hr">` 包裹原生 `<hr>`。选中态 `outline: 1px solid var(--md-primary-color)` 克制(比 image 的 2px 细),因 hr 本身已是视觉分隔线。**markdownIO 无感**:hr 无 attrs,round-trip `thematicBreak` ↔ `hr` 早已完整,schema 改 `selectable/atom/defining` 不影响序列化。`EditorInner.vue` 的 `allPlugins` 在 `imageInlineViewPlugin` 之后推入 `hrNodeViewPlugin`
- **atom 节点拖蓝(range 选区)跨越时也显示选中态**:image / hr / math_block / html_block 的 NodeView 通过 `nodes/selectionSync.ts` 共用选中态同步逻辑。`syncSelected()` 用**三通道判断**:DOM 选区纵向重叠(`Range.getBoundingClientRect()` 与节点矩形纵向比较)、PM state 重叠(`sel.to >= pos && sel.from < pos + nodeSize`)、鼠标触碰(`hasNonCollapsedSelection() && ((isDragging && mouseInNode) || (!isDragging && dragEndedOnNode))`)。**核心设计**:(1) 拖蓝期间不检查 PM state——PM 对 DOM selection 的同步是异步 rAF,拖蓝时 state 滞后,`isPmSelectionOverlapping` 读到过时数据会导致图片取消选中滞后;(2) 所有 contenteditable=false atom(无论 inline 还是 block)都有选区端点无法进入节点的问题——旧方案用 `blockAtom` 区分 inline/block 是错误的,image 同样需要 mouseInNode 补丁;(3) **粘性标志 `dragEndedOnNode`**——mouseup 时鼠标在节点上则标记,保持选中直到选区变为 collapsed(用户点击别处)或新 mousedown。解决 block atom 松开后选区端点停在前一段末尾(`pos-1`)导致 `isPmSelectionOverlapping` 返回 false 的问题;(4) `mouseleave` 非拖蓝时不触发 `syncSelected`——让粘性状态保持到选区真正变化;(5) **mouseup 扩展 PM 选区**(`extendSelectionToNode`)——松开时鼠标在节点上,双 rAF 后把 PM 选区的 `from`/`to` 扩展到覆盖 `[pos, pos+nodeSize]`,使 Delete/Backspace 走 PM 默认 range 删除时连节点一起删。不扩展的话选区端点停在节点边界,节点不在选区内 → 按删除只删文字不删节点。`dragEndedOnNode` 在扩展前 bridging 视觉态,扩展后由 `isPmSelectionOverlapping` 接管;(6) **拖蓝期间扩展 DOM 选区**(`extendDomSelectionToNode`)——快拖到 `contenteditable=false` atom 上时,浏览器无法把选区端点放入节点内部,选区端点卡在拖动起点附近,中间文字不被浏览器高亮。`mouseenter` + 节点内 `mousemove` 用 `Selection.extend()` 把焦点移到节点边界(`pos` 或 `nodeEnd`,按 anchor 位置判断拖动方向),让浏览器即时高亮从 anchor 到节点边界的全部文字。`mousemove` 持续 re-extend 因为浏览器每次 `mousemove` 从 anchor 重新计算选区会覆盖我们的扩展。**三个坑**:(1) `selectionchange` 延迟 rAF——PM 的 selectionchange 监听器注册更早,其 rAF 先执行同步 state,我们的 rAF 排在其后读到已同步的 state;(2) `mouseup` 用双 rAF——单 rAF 时 PM 可能尚未同步,双 rAF 确保第一帧 PM 调度同步、第二帧同步完成;(3) 只判纵向不判横向——浏览器把选区映射到 atom 位置时 `getBoundingClientRect()` 返回的矩形只到节点左边缘,横向重叠会漏判。**math_block 特殊处理**:编辑态(`skipSelected: () => editing`)下跳过选中框,避免与 textarea 编辑 UI 视觉冲突;html_block `stopEvent: () => true` 使 PM 不处理其内部事件(点击不产生 NodeSelection),选中态仅由 range 选区重叠 + 鼠标触碰驱动。html_inline 不参与选中态(行内 HTML 暂不做此功能)
- **NodeView async render 的 stale-check 分工**:`renderKatex` 在 `await getKatex()` 前后各判一次,**但分工不同**:入口闸只判 `is-editing`(`$$`+Enter 自动进 math_block edit 这条路径需要);await 后的闸额外判 `!isConnected`(PM 已销毁 dom 的话放弃写入)。**坑**:入口闸不要判 `!isConnected` —— NodeView 工厂同步跑 showDisplay 时 PM 还没把 dom 挂到 view.dom,`isConnected === false` 是常态,入口闸 return 后整个 NodeView 寿命里 katex 都不再 render,用户感知"math 节点消失 / 切到源码模式再切回后空了"。math_inline 去 atom 后 showDisplay 不再有"覆盖刚挂好的 editor"的 race(contentDOM 由 PM 接管,没有 editor mount 时序),但 is-editing 入口闸对 math_block 的 autoEditMathBlocks 路径仍有意义 —— 保留
- **粘贴 text/plain 必须注册 `clipboardTextParser`**: ProseMirror 默认 plain-text fallback 把整段按 `\n+` 拆 `<p>` 再 `normalizeSiblings` 自动包 blockquote,产出错位 doc。`markdownPastePlugin` 走 fromMarkdown 输出**封闭 slice `(0,0)`**(非 `maxOpen`)走标准 "join 前后 paragraph" 路径把 blocks merge 进 doc 顶层
- **样式分层**: ProseMirror 基础排版内联 `<style>`,公式/Mermaid/脚注/TOC 走 SCSS partial
- **TOC 目录走 Decoration.widget 不走 NodeView**: 跟 mermaid 同范式;widget key 含 headingsHash,变化自动重建。**坑**: `[TOC]` 回写 toMarkdown 必须用 mdast `html` 节点(非 text)包裹,text 节点里的 `[` 会被 escape 成 `\[`
- **源代码模式**: `SourceModeEditor` 独立 CodeMirror 6 `EditorView`,与 `ProseMirrorEditor` 经 `v-if` 互斥;`documentStore.sourceMode` 唯一开关。extensions: 持久行号 + 软换行 + drawSelection + highlightSpecialChars + history + 自定义 keymap(Tab 插 2 空格覆盖 `indentWithTab`;Escape → `toggleSourceMode`)+ `forbidFileDropPaste`(v0.5.1 起分叉:文件树拖入路径(自定义 MIME `application/x-velo-tree-path`) → .md 打开 / 图片落盘插图;OS 文件型 drop 图片同样落盘插 markdown 语法,与富文本行为镜像;非图片文件 drop preventDefault 防 webview 导航;paste 仍保持吞 image/*——源码模式 paste 无"树路径"概念。共享逻辑见 `image/treeDrop.ts`)+ shiki 高亮 ViewPlugin(`shikiCmPlugin.ts`,token.offset 即 CM6 doc pos)+ updateListener(docChanged → emit content,docChanged/selectionSet → emit raw markdown cursor)。**主题名镜像在 StateField**,build 只读镜像不读 store(防 ensureTheme 未 resolve 期间全黑);dark/light 纯 CSS,切主题才 rebuild。**markdown grammar 启动期不预装**(BASELINE_LANGS 不含,源码模式独用);ViewPlugin 只听 docChanged / setShikiTheme effect,首帧 grammar 未装返回空后**不自动 rebuild**(PM 侧有 `setDecorationRebuildCallback` 钩子,CM6 侧没接)→ 首次进源码模式无高亮直到敲字符,故 codeBlockReady 后 fire-and-forget 预加载 + onMounted `await ensureMarkdownGrammar()` resolve 后 dispatch rebuild effect 双保险
- **编辑器光标上报**: 状态栏所需行列由当前挂载编辑器向 `App.vue` 上报,不写进 `documentStore`。CM6 源码模式用 raw markdown 坐标;ProseMirror 模式用 `doc.textBetween` 的可见文本投影坐标,不做 raw markdown 反向映射,避免把状态栏耦合到跨模式同步算法
- **WYSIWYG code_block 行号走 per-line Decoration.widget(v0.5.11)**: 每行行首插 `<span class="velo-code-lineno">N</span>` widget(side:-1),mermaid 跳过。**不能**用 `Decoration.inline` 包裹每行代码内容做成 inline-block:shiki 的 per-token `Decoration.inline` 会与 wrapper 重叠,ProseMirror 按 token 边界把 wrapper span 拆成多个独立 inline-block,折行时在 token 之间断裂。行号走不透明背景(`--shiki-light-bg` / `--shiki-dark-bg`)遮挡横向滚动时滑入 gutter 的代码(固定 gutter 范式)。fold 范围内的 code_block 跳过(祖先折叠 → pre display:none → 行号无可见锚点)
- **跨模式光标 + 浏览状态同步**: App.vue 单点 `watch(sourceMode, cb, { flush: 'pre' })` 覆盖全部切换入口(Ctrl+\` / 工具栏 / Esc 都走这一个布尔翻转)。`flush:'pre'` 读**出**方向 view(卸载在 render 阶段,晚于 pre-flush watcher)抓锚点;`await nextTick` 后**入**方向 `onMounted` 已建 view → 应用。`crossModeSync.ts`: 两边各 token 化(剥 markdown 标记字符 `#*~_\`-+[]()!>|`——**`|` 入集关键**,否则无空格表格粘成一个 token),`captureAnchor` 取光标 ±64 个 token 序列 + token 内字符偏移,`applyAnchor` 跑 **LCS** 对齐(链接 URL、表格 `|`/`|---|` 分隔行是 CM6 多出、PM 没有的 token,整窗 indexOf 砍不掉 → 失败跳顶;LCS 当"未对齐"跳过)。光标 token 自身是多余方(如落在 URL 里)→ 退最近对齐邻居边界。最佳努力:空文档/view 未就绪放弃;LCS 矩阵超 4M 格(token > ~31k)退线性首现。滚动:CM6 `scrollIntoView(pos,{y:'center'})`;PM **不用** `tr.scrollIntoView()`(默认"最小滚入视口"= 跳到最底),改 `coordsAtPos`+祖先 `scrollBy` 居中;入方向主动 focus
- **math_inline 走 Obsidian Live Preview / Typora 风格(去 atom + contentDOM + selectionchange)**:v0.5.9 起 math_inline 从 `atom: true` 改为 `atom: false`,NodeView dom 结构 = `[prefix $] [contentDOM(PM 自管 source 文本)] [suffix $] [katex 渲染层]`,渲染层**始终可见**,`data-mode` 只控制 source + 前后 $ 是否显示:
  - `display`(selection 不在节点内):渲染层显示,source + 前后 $ 隐藏 → 阅读纯净(仅看到渲染后的公式)
  - `edit`(selection 在节点内):渲染层**保留可见** + source + 前后 $ 显示 → 源码与渲染预览并列,源码变化时由 `update()` 钩子实时把 katex 重渲染到 `.math-inline-display`(用户键入 `x^2` 立刻在右侧看到渲染后的 x²,而非裸文本)
  - `$` 分隔符走主题色 `var(--md-primary-color)`,SCSS 控制
  模式切换由 NodeView 监听 `view.dom.ownerDocument.selectionchange` 触发,读 `view.state.selection.$head` 判 `pos > getPos() && pos < getPos() + nodeSize` 决定。**与 footnote_reference "label as text content" 修复同范式**:contentDOM 由 PM 接管,光标自然进入节点内,Backspace/Delete 按节点内 selection 处理;不再有自管 input/textarea,不再有 stopEvent / isolateInput / async stale-check / autoEditMathBlocks 一堆补丁 —— 这些都只为旧的"atom + input 编辑壳"服务。**坑**:
  - contentDOM 必须是 `contenteditable=true` 的子元素(PM 默认行为,挂 dom 时设置);prefix/suffix 显式 `contenteditable=false`,防止 PM 误把光标锁在 `$` 上;`.math-inline-display` 也显式 `contenteditable=false`,防止光标从 source 漂到渲染层导致选区错位 / IME 在渲染层启动输入
  - `update()` 钩子的 `valueChanged` 分支**不能判断 mode** —— edit 模式下源码变化也要重新 `showDisplay()`,否则用户键入时只看到 `$x^2$` 裸文本看不到渲染结果,完全失去 Live Preview 体验
  - `code: true` 保留 —— 选区不可"跨" math_inline(防从外部文本选到公式中间又选到外部文本)
  - source text 由 PM 通过 `tr.insertText` 直接维护(`$x$` 段中键入 → `inlineMathSyntax` 把 `from..to` 替换为 `math_inline` 节点 + text content),NodeView 不再有任何 source 写回的 dispatch
  - 空 math_inline(`textContent === ''`)在 display 态显示 `.math-empty-placeholder`,在 edit 态 source 仍可见,用户点击进入即可键入
  - selectionchange 是 document 级事件,每个 NodeView 注册一个 listener(典型 doc 内 inline math 数量 < 50,开销可接受);密度上升可改为单 plugin + WeakSet<NodeView> 集中分发,API 不变
- **katex/mermaid 懒加载拆 chunk**: `MermaidDecoration.ts` / `MathNodeViews.ts` 顶部不走静态 `import mermaid/katex`,改模块级 lazy getter(`getMermaid()`/`getKatex()`),首次渲染时才 `await import(...)`,Vite/rolldown 据此拆出独立 chunk,首屏不加载。`vite.config.ts` 不能加 `codeSplitting:false`(会把所有 JS 合回主 chunk,懒加载失效);`cssCodeSplit:false` 保留(单 CSS 有意),代价是 katex CSS(~23KB)仍合并进主 CSS,可接受。导出路径(`lib/export/{katexHtml,mermaidHtml}.ts`)同范式 lazy getter,`htmlRenderer.ts` 调用点加 `await`。doc 含 mermaid/$$ 时 plugin/NodeView 第一次扫描到节点自动触发懒加载,加载期间 mermaid 显示"渲染中..."占位、math 显示空占位,加载完渲染。**坑**:dev 模式下懒加载几乎无收益甚至轻微回退(Vite 预构建缓存 + 首屏 doc 为空不触发渲染),production 才有真实收益(主 chunk 14MB→1.5MB,WebView2 解析编译开销骤降)——**性能指标必须用 `tauri build` 出包后在 WebView2 测,不能看 `npm run dev`**
- **块级折叠(v0.5.12 heading / list_item)**: 折叠 = 视觉层做的事,**不动 doc**——折叠点(heading/list_item/code_block 的 contentStart)写入 Plugin state 的 `Set<number>`,plugin `props.decorations` 在 fold 区段内每个 block 挂 `Decoration.node(p, p+nodeSize, { class: 'velo-folded' })`,SCSS `.velo-folded { display:none }` 接管隐。**markdownO 完全无感**(fold区段只是被 class 隐藏,toMarkdown 照常输出)。**plugin apply 阶段同步 module-level Set**(含 `foldedCodeBlockPosSet` + `foldedMermaidPosSet` + `foldedTocPosSet`):PM 渲染顺序是 reducer chain → `plugin.decorations(state)` → DOM → `view.update(view)`,`codeLineNumberPlugin.decorations` / `MermaidDecoration.buildDecorations` / `TocDecoration.buildDecorations` 必须在同一帧看到最新 set——`codeLineNumber` 的 gutter 是 `position:absolute` 浮在 `<pre>` 外,pre 被 `display:none` 时 gutter 会飞向 (0,0)或残留,只有 apply 阶段(早于 decorations 跑)清空才能让折/展开帧 gutter 跟着走。同理 mermaid 的 SVG widget 是 pre 的 side:-1 sibling、toc 的 widget 是 block-level sibling,都不继承 `display:none`,必须在 decorators 里显式 skip 创建(`isMermaidFolded(pos)` / `isTocFolded(pos)`),否则 widget 会孤零零浮在 fold 区段外。**折叠区段只挂完全在 range 内的 block**:`doc.nodesBetween(from,to)` 会访问跨 range 的 ancestor(折叠 list_item 内段落,nodesBetween 把外层的 bullet_list / list_item 也算进回调),一旦外层容器被挂 `velo-folded` → 整个 list 消失。改为自写 walk,只处理 `childStart >= from && childEnd <= to` 的 block。**toggle widget key 不含折叠状态**(只含 pos:stableKey),所以 collapsed↔expanded 切换时 PM 复用旧 button DOM,factory 不重跑、chevron 不旋转——placeholder 展开路径必须手 find `data-fold-key` 元素并 `setAttribute('data-fold-state')`,让 CSS transition 察觉到变化触发 0↔-90deg 旋转。`list_item` 折叠要求 childCount>1(首段之后有 block 子项),单条叶节点不加 toggle。**code_block 折叠**:与 heading/list_item 同范式——header widget key **不含**折叠状态(`code-header:..:..:..:${isWrapped}`,只含 isWrapped),fold toggle 复用旧 header DOM + click handler 手动翻 `data-fold-state`(让 CSS transition 播放 chevron 0↔-90deg 旋转,类 heading 的 `fold-toggle` / frontmatter 的 `velo-frontmatter-fold-btn`);fold-info 可见性由 CSS `[data-fold-state]` 随 attribute 翻转,无需重挂。若 fold 写进 key,toggle 销毁旧 widget 建新的 → 新 DOM 一开始就是终态 → 只会"闪烁一下直接变"。toggle 由 codeHighlightPlugin 的 header widget 内置 chevron 提供(click → dispatch setMeta(foldKey, { toggle: contentStart }))。mermaid code_block 折叠时 foldedMermaidPosSet 同步更新 → MermaidDecoration skip SVG widget。空 code_block 也可折叠(header 始终渲染 chevron,空 pre 仍占 padding/border/margin 视觉空间,折叠应隐藏它);`addCodeBlockDecos` / `collectFoldableKeys` 不因 `content.size===0` 跳过

- **阅读模式(readOnly)禁用"展开源码"交互 + 隐藏编辑类按钮**: `view.editable=false` 只挡用户输入,不挡 `appendTransaction` / `handleClick` / NodeView 事件。"光标进入/点击 → 显示源码字符"的交互(mark 源码编辑 / 链接单击 / 图片按钮 / 行内与块级公式编辑态 / mermaid toggle)在每个触发点读 `view.editable` 守卫;编辑类按钮(code 语言切换 / mermaid delete+toggle / TOC delete / task checkbox)用 CSS `.ProseMirror[contenteditable="false"]` 隐藏或禁点击,走 contenteditable 属性动态生效(无需 rebuild decorations / NodeView)。**坑**:`markSourceEditPlugin.appendTransaction` 无 view 参数,用模块级 `editorView` 引用(plugin spec `view()` 设、`destroy` 清)读 editable。保留:链接 Ctrl/Cmd 跳转、`ensureMermaidSourceVisibleAt`(查找替换)、复制按钮、TOC item link、footnote backref(导航 / 非编辑类)。脚注无 source/display 切换(label 是 text content),不纳入

- **专注模式 CSS 驱动 + plugin 只标记当前块**: `focusModePlugin` 开启时用 `Decoration.node` 给光标所在的**顶层块**(depth-1 祖先,blockquote 内段落高亮整个 blockquote)挂 `.velo-focus-active` class,降透明度由 CSS `.velo-editor.focus-mode .ProseMirror > *` → `opacity:.25` + `.velo-focus-active` → `opacity:1` 接管。plugin 不自己 dim 非活跃块(避免在每次光标移动时重建大量 Decoration),只管标记当前块一个 node decoration。容器 class(`.focus-mode`)由 `ProseMirrorEditor/index.vue` 的 prop 驱动,与 plugin 的 enabled state 经 `setMeta(focusModeKey)` 同步。源码模式(CM6)镜像同设计:`cmFocusModeField` StateField 跟踪当前段落(空行分隔),用两条 `Decoration.mark`(段前 + 段后)降透明度,O(1) decoration 量

- **打字机模式居中 defer 到 `requestAnimationFrame`,同步 scrollBy 会被覆盖**: `typewriterModePlugin` 开启时把光标所在行滚到滚动容器(`findScrollAncestor`)垂直中线。**关键坑**:不能在 plugin `view.update()` 里**同步** `scrollBy` —— `updatePluginViews()`(行 5527,plugin `view.update()` 在此跑)先于 PM 的 scroll 分支(行 5530-5538:`scrollToSelection` / `resetScrollPos`),也先于浏览器原生 `overflow-anchor`(布局期);同步 scrollBy 落在所有这些之前,会被带 `scrollIntoView` 标记的 tr(Enter / paste / find 命中 / TOC·大纲跳转 / crossModeSync placeCursor)的"最小滚入视口"或浏览器 scroll-anchoring 覆盖,光标被顶出视口 → "光标消失"。解法:居中走 `requestAnimationFrame`,在 PM 整个 `updateState` 返回之后、paint 之前跑,scrollBy 是最终结果(无抖动);同帧多次 update 只排一个 rAF,回调里读最新 state。**仍需 `props.handleScrollToSelection` 开启时返 `true`**:PM 的 `scrollToSelection` 不只滚编辑器容器,可能滚 window / 外层祖先(`scrollRectIntoView` 沿链向上),rAF 只居中编辑器容器撤不掉 window 滚动,故先抑制 PM 自身滚动(行 5546 `someProp` 命中即跳过)再 rAF 居中;关闭返 `false` 零回归。**不用 `scrollThreshold: Infinity`**:静态值无法随 enabled 翻转,`view.setProps` 又触发重挂。居中触发条件:`view.update()` 里 enabled 且 sel/doc 变化或 just-enabled(toggle-on 立即居中,defer 一帧),meta-only tr(find 高亮 / fold toggle)跳过。居中算法取选区垂直中点(collapse 选区取 head 行中线;NodeSelection 走 `nodeDOM` 真矩形 —— 见下条;范围选取 `from.top`→`to.bottom` 中点),祖先 `scrollBy(delta)`,阈值 4px 防亚像素抖动。**边界(有意接受)**:切文件后 `resetScrollToTop()` 覆盖居中(光标落顶,下次交互再居中),切标签 `restoreScrollTop()` 同理 —— 与"打开文件不抢视口"取舍一致。源码模式(CM6)镜像同坑同解:`EditorView.updateListener` 里**不**同步 `view.scrollDOM.scrollBy`(会被 CM6 自身 scrollIntoView 覆盖),改 `scheduleCenterCm` defer 到 rAF;enabled 用组件级 `let` 而非 StateField(typewriter 无 decoration),`onBeforeUnmount` 取消 pending rAF

- **打字机居中 NodeSelection 用 `nodeDOM` 真矩形,不能用 coordsAtPos(from/to) —— 修点击图片顶 ~1/4 视口**: 点击图片产生 NodeSelection。Velo 图片是 inline-atom,但 NodeView 的 wrapper 是 `display:block` 撑满行(模型 inline、渲染 block);`coordsAtPos(from)` 落在图片**上方**文本行、`coordsAtPos(to)` 落在图片**下方**文本行,二者中点偏向上下文本行而非图片本体 → 居中把图片往上顶约 1/4 视口。改用 `view.nodeDOM(sel.from).getBoundingClientRect()` 拿图片 wrapper 真实矩形取中点。collapse 文本选区 from===to 退化 head 行中线,范围选取 from.top→to.bottom 中点,行为不变

---

## 维护者注意点

- **表格 cell 对齐 / attrs 修改:$from.before(d) 而非 $from.start(d)**:向上遍历找 cell 时(`setCellAlignment` 等 per-cell attr 修改),`$from.start(d)` 是 cell 内容区起点(= cell open token + 1),`setNodeMarkup(内容起点)` 会写到 cell 内部的 paragraph 而非 cell 本身 → cell attrs 静默不更新。空 paragraph 内光标 `depth` 落在 cell 层(paragraph 不增加 depth),`start(d)` 与 `before(d)` 差 1,这个错位被放大。正确用 `$from.before(d)`(cell open token 所在 pos,与 `descendants`/`nodeAt` 返回的 pos 同语义)。调试首选用 `$from.before(d)` 打印每一层,确认与 `nodeAt` 取到的 cell pos 一致
- **表操作命令(cmdAddRow/deleteColumn 等)绕开 prosemirror-tables 的 findTable**:Velo 定制 `table` 节点后(`content: 'table_header_row table_row+'` + `isolating: true` + 强制 `table_header_row` 只含 `table_header`、`table_row` 只含 `table_cell`),`prosemirror-tables` 的 `findTable($from)` 内部按列宽(`map.width`)偏移定位 cell,返回错引用;拿它的 `start` 做 `replaceWith(from, to)` 切片错位,产生**伪旧表@0(size6) + 真新表@6(size32) 并列**的结构损坏。`findTableWrapper` 改为纯手动沿 `$from` 向上找,并把 `pos` 对齐到 `descendants` 语义(`$from.start(d) - 1`,即 open token 前的 gap position);`replaceWith(0, nodeSize)` 与 `replaceWith(1, nodeSize+1)` 差 1 就是单表 OK vs 双表 broken 的分界。`rowIndexForSelection` 同步改 `tablePos + 1 + offset === rowAbsStart - 1` 匹配。调试手法:dispatch 后 `doc.descendants` 首个 table 应为正常 size、无并排伪表;`headerRowCount` / `bodyRowCount` 用 descendants 计数可靠
- **prosemirror Fragment.forEach 的回调第二个参数是 offset 不是 index**:`row.forEach((node, offset, index) => ...)` —— 第二个参数 `offset` 是节点在父 fragment 内的位置偏移(如 0/5/10),**第三个参数 `index` 才是 0-based child index**。`findColIndex` / `cmdAddColumnAfter` / `cmdAddColumnBefore` / `cmdDeleteColumn` 里所有按列筛选的 `forEach((cell, i) => ...)` 必须用第三个参数 `index`,用 `i`(offset) 会让 colIdx 变成 offset 值(如 10),导致 `i === colIdx` 永远不命中 → 删列实际没删 / 加列位置错。症状:删列后 header 保留 3 列而 body 变 2 列(错位)、或只有 cursor 所在行被改其他行不动。`rowIndexForSelection` 里的 `table.forEach((_c, offset) => ...)` 用 offset 是对的(它用 offset 算 descendants-pos),但 index 单独用 `idx++` 累加 —— 不要混淆
- **脚注 label 是显示文本,无自动编号**: 扩展点是在 `FootnoteNumberPlugin.state` 加 `numbering: Map<label, number>`,**不要**把编号写回 label(丢语义,跟 GFM 不符)。`footnote_reference` 的 label 是 text content(schema `content: 'text*'`),`footnote_definition` 的 label 是 `attrs.label`
- **空 math 节点走占位不调 katex**:`MathNodeViews.ts` 的 `showDisplay` 在 `attrs.value`(block)/ `textContent`(inline)为空时改渲染 `.math-empty-placeholder`(虚线框 + 提示文字),不走 `renderKatex('' → ' ')` —— katex 输出高度趋近 0 的 `.katex-display`,WYSIWYG 里节点看起来"消失",源码模式却仍然能看到 `$$\n$$` 两行。math_block 占位 `pointer-events:none`,点击事件透传到 `.math-node` 上的 click listener → 重新进 `startEdit`。math_inline 占位同理,但 selection 一旦进入 math_inline 内(空节点)就走 selectionchange → data-mode="edit" → source 显示出来直接键入,不再需要 click → startEdit 的二段交互。`startEdit`(math_block)里 `setPreviewHtml(renderedHtml)` 也要按此短路:空节点捕获到的 `renderedHtml` 是占位 DOM,塞回 preview 会让 `querySelector('.math-empty-placeholder')` 重新命中 → 必须 `if (renderedHtml && node.attrs.value) editor.setPreviewHtml(renderedHtml)` 守卫。
- **shiki 两条正交路径**: darkMode toggle 纯 CSS 切色(零重渲,不要 dispatch setMeta);换主题 hex 变了才 rebuild,由 App.vue watch 触发。懒加载 lang(`ensureLanguage`)/主题(`ensureTheme`)只 append grammar/hex **不重建 highlighter**,走 plugin 端 `setDecorationRebuildCallback` 钩子让 plugin 自己决定 rebuild 时机;**不要 await getHighlighter 后立刻 dispatch setMeta**。CM6 源码模式同理:主题名镜像在 StateField,build 只读镜像(防 ensureTheme 未 resolve 期间全黑)
- **clipboard 统一走** `@tauri-apps/plugin-clipboard-manager` 的 `writeText`
- **code header 标题栏**: `Decoration.widget(pos, toDOM, { side:-1, stopEvent: () => true })` 渲染在 `<pre>` 之前(兄弟节点),正常文档流 block 元素,始终可见(非 hover-gated)。header 与 pre 视觉连为一体:header 上圆角 + pre 下圆角,共享 border/bg(`.velo-code-header-widget + pre` 去掉 pre 上边框 + 上圆角)。折叠时 pre 挂 `velo-folded`(display:none),header 用 `:has(+ pre.velo-folded)` 变独立卡片(全圆角 + 下边框)。header 内含折叠 chevron(CSS rotate 动画)+ 语言输入框(只保留下 border 的 input,非按钮 + 浮层;focus / 输入时在 body 上挂 fixed 定位下拉候选项,Enter / click / blur 提交,Escape 还原)+ 行数摘要(折叠态显示)+ 复制按钮。语言选择器从旧按钮 + Vue 浮层方案改为内嵌 input + 命令式 dropdown,消除了跨组件事件通信(`velo:open-lang-picker` CustomEvent)和 `CodeBlockLanguagePicker.vue` 组件。**widget spec 必须同时设 `stopEvent: () => true` + `ignoreSelection: true`**:header 内嵌了原生 `<input>`,input 获得焦点时浏览器改变 DOM selection → PM 的 DOMObserver 检测到选区变化反推编辑器选区 → 光标被拉出 input。`ignoreSelection: true` 拦 DOMObserver 的 selection flush 路径,`stopEvent: () => true` 拦 PM event handler 路径(mousedown → `handleClick` / `handleSelectionChange`),两道闸缺一不可(与 math_block NodeView `stopEvent` 同源坑)。**第三道闸:langWrap 的 click 必须 `stopPropagation`**——`index.vue` 外层卡片有 `@click="onCardClick" → focusEditor()`,click 不拦会冒泡到卡片 → 焦点被抢回编辑器 → input 失焦(光标"自动移出"根因)。fold/copy 按钮在 click 上也 stopPropagation 了,同因。按钮(fold / copy)自己 mousedown preventDefault + stopPropagation,不依赖 stopEvent,返回 true 对它们无影响
- **code_block 自动换行(wrap)**: per-block 运行时视图态,不进 schema / markdownIO(round-trip 无感)。`codeWrapPlugin` 独立 plugin,**默认开启**——跟踪的是 *已关闭* wrap 的 code_block(`unwrappedSet`,存 node pos),空集 = 全部开启;新创建的 code_block 不在 set 中 → 自动 wrap。与 fold 同范式(module-level set 供跨 plugin 读、apply 阶段 mapping + sync、`Decoration.node` 挂 `data-velo-wrap="true"`)。toggle 按钮在 header widget 内(复制按钮左侧,`margin-left: auto` 推到右侧),wrap 开启 / 关闭时图标切换(wrap-text ↔ nowrap)+ active 态主色高亮。SCSS `pre[data-velo-wrap="true"]` → `white-space: pre-wrap; overflow-wrap: break-word; overflow-x: hidden`。header widget key 含 wrap 状态 → toggle 时 PM 重建 header(图标 + active 态正确)。**行号交互**:wrap 模式下 gutter widget 用 Range API 逐行测高 + set div height;gutter widget key 也含 wrap 状态 → 同步重建。不持久化(切文件重置,全部恢复默认开启)
- **代码块语言图标**:语言输入框与下拉候选列表均渲染单色语言图标(与 `@lucide/vue` 等其他 UI 图标同 currentColor 单色契约,协调)。图标来源:`src/components/ProseMirrorEditor/nodes/lang-icons/*.svg`(本地文件夹,主要是 nonicons 开源图标库 17 个品牌视觉,单色 currentColor 风格,16×16 画布)。`nodes/langIcons.ts` 用 Vite 的 `?raw` 直接 import 这 17 个文件为字符串,加别名表 + lucide Code(< > 双尖括号)兜底,无构建期抽取脚本。**为什么维护本地文件夹而不是 npm 包 / iconify 运行时**:Tauri 桌面应用 "本地打包无 CDN" 契约禁运行时网络,iconify 运行时走 CDN 违背;且本地文件夹丢文件即扩展、无版本漂移、可直接 git diff 图标改动,比 `@iconify-json/*` 镜像更稳。**键对齐 `LANG_OPTIONS` 唯一真源**,别名复用同一文件(jsx→javascript、tsx→typescript、scss→css、sass→css、less→css、shell→bash、dockerfile→docker)。未命中品牌(含 plain text / bash / shell / powershell / sql / xml / markdown / diff / makefile / mermaid)走 lucide Code 兜底。**图标查表前先归一化**(`normalizeLang`:小写 + `LANG_ALIASES` 简写表,如 js→javascript、py→python、md→markdown、sh→bash、docker→dockerfile;text/txt/plaintext→空)—— shiki 高亮路径已自带 lowercase + alias 路由,图标层做的是裸字符串查表,不归一化就会"高亮对、图标错"显示 fallback 图标(文档 fence 用了 `js` / `Python` / `md` 等别名或大小写变体时踩此坑)。新增别名 = `LANG_ALIASES` 补一行。新增语言 = 往 `lang-icons/` 丢一个 `<lang>.svg` + `langIcons.ts` 的 `LANG_ICON_BRAND` 补一行,重跑构建;input / dropdown 代码不动。
- **``` 语言建议下拉(codeBlockLangSuggestPlugin)**:用户在 paragraph 中键入 ``` + 语言前缀时,在光标下方浮出模糊匹配下拉(复用 `LANG_OPTIONS` + `langIconSvg` + `.velo-lang-dropdown` 样式,与 code_block header 的语言输入框下拉同构)。**为什么走原地下拉而非输入 ``` 即转 code_block**:PM decoration rebuild 跨帧异步,即转 code_block 后无法可靠地在下一帧前把焦点重定向到 header 语言输入框,用户连续输入 ```python 时 `python` 会落入 code_block 内容区。原地下拉是纯视觉叠层,用户始终在 paragraph 内输入,提交时一次性转 code_block,无竞态。**与 codeBlockEnterCommand 的分工**:plugin 的 `handleKeyDown` 必须排在 keymap 之前(basePlugins 首位);有键盘高亮条目时 Enter 拦截并提交选中语言(用户输入 `p` 选中 `python`),无高亮时 return false → keymap Enter 链的 `codeBlockEnterCommand` 正常触发(用原始文本提取 lang)。Escape 设 dismissed 标志关闭下拉,docChanged 时清零(再输入恢复)。plugin state 在 `apply` 中对每笔 tr 做 `detectSuggest`(选区空 + paragraph + 光标在段末 + 文本匹配 `^```[ \t]*([^\s`]*)[ \t]*$`),DOM 下拉在 `view` factory 的 `update` 中按 state diff 创建/更新/销毁(挂 body + fixed 定位 + scroll/resize 跟随)
- **快捷键 declarative registry**: 所有键位在 `editor/shortcuts/bindings.ts` 集中注册,**不**在 EditorInner.vue 硬编码。新加快捷键 = 新建 command 文件 + `bindings.ts` 加 1 行 `registerShortcut(...)`,不碰 EditorInner / registry.ts
- **inline syntax regex 必须自带 word boundary**,不依赖 registry 顺序防误识别: 开口 `(?<!\W)` / 闭口 `(?!\W)` 等挡前后导;inner 不含分隔符(如 strong inner `[^\n*]+?`)。例:`**33**` 必须被 strong 吃掉、`text==hi==` 不应被 highlight 误识别 —— 见 `syntax/inline/strong.ts` / `highlight.ts` 顶部注释
- **mark 语法闭合后必须 `removeStoredMark`**: `**bold**` / `*it*` / `==hl==` / `~~st~~` / `_em_` / `` `code` `` 的 apply(`delete → insertText → addMark`)后,光标停在 inner 末尾 = inclusive mark 右边界。这些 mark 的 `inclusive` 默认 true(schema 未设,与 `link` 的 `inclusive:false` 不同),且 apply 未设 storedMarks(=null),继续输入时 PM 回退到 `$from.marks()`(含该 mark)→ 继承粗体/高亮。修复:apply 末尾 `tr.removeStoredMark(markType)` 让 storedMarks=[] 覆盖继承(storedMarks 优先于 `$from.marks()`,故 inclusive 边界也能退出;与 Typora/Obsidian 一致)。**不要**改 `inclusive:false` 解决 —— 会破坏 Ctrl+B 连续输入(`markCommands` 的 `addStoredMark` 首字符消耗后,后续字符靠 inclusive 边界继承);`link` 能用 `inclusive:false` 因它不靠 Ctrl+B 连续输入;`code` mark `excludes:'_'` 独占无 Ctrl+` 命令,但仍走 `removeStoredMark` 保持一致。**这只覆盖"语法刚闭合那一笔"**;光标进入已有 mark 范围继续编辑由 `markSourceEditPlugin` 处理(见下条),两者互补不重叠
- **mark 源码编辑 session(Obsidian Live Preview 风格)**: `markSourceEditPlugin` 让光标进入 `**bold**` / `*it*` / `==hl==` / `~~st~~` / `` `code` `` 范围时,整段换成可编辑的 markdown 源码字符;移出光标 commit 还原成 mark。session 状态机(apply mapping bias +1/-1 + pendingCommit + `view().update` 触发 commit + Escape 还原)逐字照搬 `linkClick.ts` / `imageEditPlugin.ts`。
  - **触发用 `appendTransaction` 不是 handleClick**:光标进入即换源码。**保命守卫**:仅 batch 含 `selectionChanged && !docChanged` 的 tr 才触发 —— 键入 / IME / 粘贴是 `docChanged`(即便也移光标)不触发,保住 Ctrl+B 连续输入(`inclusive:true` 必须留,`addStoredMark` 首字符后靠边界继承)。commit tr 自身 `docChanged` 也不触发(双保险防重进)。仅 `resolve(head).marks()` 含目标 mark 时触发(内部 + inclusive 右边界);左边界(`head===markStart`,resolve 不含 mark)不进 —— typing 在左边界本就不继承,无需 session
  - **enter 守卫的 `isBlacklisted` 只挡 `code_block` / `math_block` 容器,不挡 `code` mark**:行内 `code` mark 本身是 session 目标,光标进入要换源码;`markCommands` 的 `code` mark 黑名单(Ctrl+B 在 code 内不切换)是另一回事,勿与 enter 守卫对齐。code mark `excludes:'_'` 独占,commit 不走 `applyMarkerWriteback`(只改 strong/emphasis),纯靠 `fromMarkdown` 重解析 `` `code` `` 还原 code mark
  - **commit 不调 `setSelection`,靠 `tr.mapping` 重映射光标**:用户已把光标移到 session 外,commit 的 delete+insert 让光标自然落在还原 mark 之外(右离开 `P-S+L >= editFrom+L+1` 过 mark 末尾;左离开 `<=editFrom-1` 在 mark 起点前,inclusive 左边界不含 mark)→ 不立即重进。**Escape 不同**:光标原本在源码内,mapping 会落回还原 mark 内 → 必须 `setSelection(editFrom)`(inclusive 左边界,不含目标 mark)
  - **`buildMarkSource` 自己拼保 `*`/`_`**:不能用 `toMarkdown` —— 它把 emphasis 恒输出 `_`、strong 恒 `**`,丢 `attrs.marker`。遍历 slice inline 节点按固定嵌套序(开:strong→emphasis→strike→highlight→code;闭反序)在 mark 转换处插 `markerText(mark)`(读 `attrs.marker`)。`**bold *italic***` → 两端 `**`+`*` 全显;`__bold__`/`_it_` 原样。`code` mark `excludes:'_'` 独占,不会与其他 mark 并存,`markerText` 恒返回单 backtick —— 多 backtick 代码(`` `` .. `` ``)进 session 降级为单 backtick(已知限制;源文件加载走 fromMarkdown 仍完整支持)
  - **commit marker 回写只改已存在 mark**:`fromMarkdown` 创建 strong/emphasis 用默认 `marker:'*'`,丢原始分隔符。commit 后只对**已带该 mark 的 text span** `removeMark`+`addMark(marker=detectMarker(source))` —— 不能对整段 addMark(会把破源码 / 改成别的 mark 类型时的 plain text 误加 strong)。`detectMarker` 用正则从源码检出 `*`/`_`
  - **`findMarkRange` 坑(inclusive 左边界)**:(a) char `[p,p+1]` 带 mark 当且仅当 `resolve(p+1).marks()` 含 mark,向前扫判 `resolve(start).marks()`(char 末位)而非 `resolve(start-1)`(左边界不含 mark 会漏首字符,delete 留孤儿)。(b) **块首专属**:`resolve(块首).marks()` 在 `nodeBefore` 为空时回退取 `nodeAfter` 的 marks(含本 mark),左扫用 `start>0` 会越过块开 token 落到块外(pos 0)→ `delete(0, markEnd)` 跨块边界,结构碎裂成 `**bold*` + 散落 `*`(commit 后变独立行 `*`)。左扫必须以块首内联起点 `resolve(pos).start(depth)` 为下界;右扫查 `resolve(end+1)` 不命中块首(nodeBefore 总存在),无需对称下界
  - **退避三处**:`syntaxAutoFormat.getActiveEditRanges` 返回全部活跃 session(link/image/mark)范围(见下条);`markCommands.forceStoredMarkOnly` 扩展含 markEditSession(session 内 Mod-b 只 `setStoredMark` 不插 `****`);link/image session 范围内不进(session 互斥,`buildMarkSource` 不处理 link 语法)
  - **多 session 并存必须全退避(翻倍坑)**:`getActiveEditRanges` 返回所有活跃 session 范围数组,`tryInlineSyntaxes` 用 `some(rangesIntersect)` 任一相交即跳过。**不能**早 return 优先一个 —— 多 session 并存时(例:link 编辑态展开期间 click strong,link session 尚未 commit、markSourceEdit 已 enter strong),若只退避 link 范围,strongSyntax 会把 mark session 的 `**bold**` 源码立即转回 mark → markSourceEdit session 经 mapping 倒置(editFrom > editTo)→ commit `textBetween`/`delete` 范围错乱 → insert 没对应 delete → **文本翻倍(每次 ×2)**。早期 `getActiveEditRange` 优先级 link > image > mark 早 return 踩此坑
  - **enter 事务挂 `SKIP_CONTENT_EMIT`**:瞬时视图切换(mark→源码文本)不回写,否则 `toMarkdown` 把源码 `**` 转义成 `\*\*` 与渲染态 mark 序列化不同 → 误判 dirty。commit / Escape 不挂(需回写把 content 同步到还原后的 mark)
  - 装入位置:`syntaxAutoFormatPlugin` **之前**(`EditorInner.vue` 的 `allPlugins`)—— appendTransaction 的 enter 让 `getActiveEditRanges` 在 pass 2 读到 session 自动退避。与 link/image escape keymap 同区;三者 Escape 各自 session 为 null 时 return false,顺序非关键
  - **行为变化(已知)**:Ctrl+B 连续 typing 到 mark 边界仍继承(旧 `handleTextInput` 无条件 redirect 不继承)。为保 `inclusive:true` / Ctrl+B 连续输入的有意取舍,属 Obsidian Live Preview 语义。粘贴进 mark 不自动进 session(paste 是 docChanged),需导航出再进
- **新增暗色规则要两处同步**: editor 走 `_editor-dark.scss` 的 `:is(.dark .velo-editor, .velo-editor.dark)`(Vue 控制 `<html class="dark">`),export 走 `exportStyles.scss` 的 `@media (prefers-color-scheme: dark)`(**自写副本,不 forward `_editor-dark.scss`**,导出 HTML 无 `.velo-editor.dark` 依赖)。两套语义不等价:导出只跟系统暗色偏好走,不能跟应用内 toggle 走。新增暗色规则必须两边写
- **shiki token 必须缓存,`getTokensCached` 是 per-keystroke 关键路径**: `props.decorations(state)` 契约无脏区间钩子,每次 transaction 全量重跑;1000 行文档对所有 code_block 同步跑 `codeToTokensWithThemes` 累计 ~100ms 卡顿,实测占输入路径总耗时 90%+。`CodeBlockLangs.ts` 走 LRU(cap 200)按 `(lang + lightTheme + darkTheme + content-hash)` 缓存 token 数组,普通段落键入 ~99% 命中,单次 deco build 从 ~100ms 降到 ~5ms。**不要清理这层 cache,删了立刻退步 20x**。缓存值是 token 而非 Decoration —— `token.offset` 是块首相对偏移,与 doc 位置无关,`buildDecorations` 仍走 `blockStart + offset` 重算绝对 pos;直接缓存 Decoration 会脏(`Decoration.inline` from/to 是绝对位置,块在 doc 里移动就过时)。theme 名进 key 自然处理"切代码块主题"路径,rebuild 触发由"shiki 两条正交路径"那条 `setDecorationRebuildCallback` 钩子统一管。
- **foldDecoration 跨 plugin set 是 module-level mutable state**:`foldedCodeBlockPosSet` / `foldedMermaidPosSet` / `foldedTocPosSet` 在 apply 阶段由 `recomputeFoldedCodeBlockPos` 整体替换(`Set` 引用 swap,不是 mutate in-place),下游 plugin (`CodeLineNumberWidget` / `MermaidDecoration` / `TocDecoration`) 在 decorations 里读 —— 不要改成 push/add,否则 apply 跑多次时 set 会累积。测试隔离:`FoldDecoration.ts` 的 module-level Set 会跨 suite 泄漏,**跨插件集成测试请放独立文件**(见 `foldCrossPlugins.test.ts` 顶部注释),不要让 dispatch-heavy 的其他 suite 跟本 suite 并行跑(已踩:`foldDecoration.test.ts` 的残留 dispatch 污染了 `foldCrossPlugins.test.ts` 的 widget 状态)
- **`initCollapsed` meta 只接受数字数组**:file 切换 / EditorInner 挂载时把 store 里的 stable key 经 doc walk 翻成 `Set<number>`(contentStart),不要直接传 stable key 字符串。`tr.mapping.map(pos, -1)` 跟住 doc 变化用 assoc `-1`(insertText 在 folding point 之前时 pos 不跑到新文本末尾);docChanged 处理要加 `m <= 0` / `$pos.depth === 0` guard,deleted block 后 folding point 落到 doc 起点会抛
- **placeholder widget 展开需要手动同步 toggle button**:因 PM widget key 不含折叠状态(DOM 复用),placeholder 点开后必须显式 find toggle 元素并 set `data-fold-state`,否则 chevron 停在 rotate(-90deg) 不动。详见上面设计要点「toggle widget key」条目
- **`selectable: false` 只挡 NodeSelection + 鼠标拖蓝**:键盘 Arrow 仍可越过 folded 区段(是 PM 不知 CSS display 的固有限),v1 有意接受;如果要挡需要把 fold 区段包 NodeView 自管 keydown,范围大不划算
- **`ensureFoldExpandedAt(view, pos)`** 幂等展开 helper,`findreplace` 命中折叠区段时调,形如 mermaid 的 `ensureMermaidSourceVisibleAt`。返回值 = 是否展开过,是则调用方应 scrollIntoView 到目标
- **htmlTag 实时转 html_inline 必须注册**(v0.5.7): 用户键入完整 HTML 行内标签(如 `<kbd>Mod</kbd>`)想 PM 立即渲染 kbd 视觉,核心就是一条:
  1. **必须注册**:`syntax/index.ts` 缺 `registerInlineSyntax(htmlTagSyntax)` 等于整条语法静默失效 —— 不报任何错,用户敲完整段都不转(已踩坑:v0.5.7 之前的 syntax/index.ts 漏注册,文档里有 htmlTag 但实际不生效)。注册位置放 inline 队列最后(`highlight` 之后),不影响其他 syntax 抢匹配
  - **只匹配完整闭合**:regex 是 `PAIRED | SELF_CLOSE`,`<TAG>content</TAG>` 或 `<TAG/>` 才转。**不**做"敲到一半就转开标签"的优化 —— 用户期望边敲边编辑,过早转 atom 反而把光标锁在 atom 之后,backspace 删不掉刚敲的 `<kbd>`,体验更差
  - **`<` 在 prose text 里的反斜杠转义是正常行为**:敲到一半的 `<kbd>` 留 plain text,`toMarkdown` 走 `mdast-util-to-markdown` 的 `safe()` 加 `\<` 反斜杠转义(prose text 里的 `<` 后接字母或 `/` 属于 unsafe 模式,CommonMark 规范要求)。完整闭合转 atom 后这条转义路径自动消失。**不要**在编辑器层去对抗 round-trip 完整性(`safe` 是为重 parse 时 `<` 不被误当 HTML 起始)

---

## 新增语法支持 checklist

> 一条新语法落地，需要 check 一遍涉及哪些层避免遗漏。
> checklist 不是规约，多数简单语法只碰其中 2-3 个文件。


| # | 涉及层 | 文件 | 何时需要动 |
|---|--------|------|-----------|
| 1 | **schema** | `editor/schema.ts` | 新增节点类型 / mark / attrs |
| 2 | **NodeView / widget** | `nodes/*.ts` 或 `editor/imageNodeView.ts` 等 | 需要特殊视觉(数学渲染 / mermaid SVG / 任务 checkbox / 链接源编辑) |
| 3 | **remark 插件**(外部解析) | `plugins/remark*.ts` + `editor/markdownIO.ts` 里 `.use()` | mdast 树需要重写(如 alert / preserveEmptyLine)或补缺 |
| 4 | **markdownIO 双向** | `editor/markdownIO.ts` | 新节点要进 fromMarkdown / toMarkdown；新 mark 要在 pmInlineToMdast 加分支 |
| 5 | **syntax registry**(实时键入) | `syntax/{block，inline}/*.ts` + `syntax/index.ts` | 用户键入也要转(块级 / 行内带匹配) |
| 6 | **ProseMirror 插件** | `EditorInner.vue` 的 `allPlugins` 数组 | 需要新装饰 / 行为插件(查找高亮 / 自动补全 / 原子保护) |
| 7 | **keymap** | `EditorInner.vue` | 新快捷键(如 `$$` + Enter) |
| 8 | **测试** | `__tests__/*.ts` | 每条语法至少 1 happy + 1 反例；`markdownIO` 改动必加 round-trip |
| 9 | **RELEASE_NOTES** | `docs/RELEASE_NOTES.md` 当前版本对应分组（发布后写） | 发版时该语法的 feat/fix 走 `git log` 即可；属用户可见变更按分组写入 RELEASE_NOTES |
| 10 | **ARCHITECTURE** | `docs/architecture/editor.md`（必要时联动 `docs/ARCHITECTURE.md` 路由） | 跨节点依赖 / 触发时机反直觉 / 新黑名单维度 / 非显然设计取舍；**纯模板化不需要改** |
| 11 | **DECISIONS** | `docs/DECISIONS.md` | 候选方案 ≥ 2 的"为什么走 X 不走 Y"取舍（非显然决策），走 ADR 格式；普通语法不进 |

**容易遗漏的项**:
- 第 5 列注册——`syntax/index.ts` 没 `registerXxx` = 不生效，且无警告，纯静默
- 第 4 列双向——`fromMarkdown` 加了，`toMarkdown` 忘了，文件保存再加载会丢数据
- 第 9 列留痕——发版时该语法的 feat/fix 走 `git log` 即可；属用户可见变更按分组写入 RELEASE_NOTES，属"重大决策"取舍另写入 DECISIONS ADR 块（"为什么走 X 不走 Y"不写在 commit message 里）
- 第 10 列过度——简单语法也写一段架构说明反而稀释文档信号

**已落地的语法参照**:
- `mermaid` 涉及 schema(`code_block { language: 'mermaid' }`，无独立节点) + remark(走 mdast code) + markdownIO 双向 + mermaidDecoration plugin(扫 code_block 渲染 SVG widget，不走 syntax)。codeHighlight 工具条 + mermaid SVG widget 双 widget 共存(不同 side)
- `alert` 涉及 schema + remark(remarkAlert) + markdownIO 双向 + syntax/block/alert + 注册
- `footnote` 涉及 schema + NodeView + FootnoteNumberPlugin + syntax/inline/footnoteRef + 注册
- `_italic` / `~~strike~~` 涉及 schema + syntax/inline + 注册(无 NodeView / 无 remark)
- `` `code` `` 涉及 schema(已有 `code` mark,`excludes:'_'` 独占) + syntax/inline/code + 注册 + markSourceEdit session(进 enter 守卫 `isBlacklisted` 需放行 `code` mark,只挡 `code_block`/`math_block` 容器)。无 NodeView / 无 remark(remark-parse 原生 inlineCode);markdownIO 双向已由 `inlineNodeToPM`/`wrapWithMarks` 处理,改 syntax 不碰 markdownIO 但 round-trip 用例已覆盖
- `[TOC]` 涉及 schema + Decoration.widget(TocDecoration) + markdownIO 双向 + syntax/block/toc + 注册(无 NodeView / 无 remark)

---

## TS class → SCSS 映射表

ProseMirror NodeView / Decoration / Plugin 在 TS 中命令式创建 DOM 并设 class 名,对应的 CSS 规则在 SCSS partial 中定义。改样式时先查此表定位 SCSS 文件。

### NodeView / Widget DOM

| TS 文件 | class 名 | SCSS 文件 |
|---------|---------|-----------|
| `CodeHighlightWidget.ts` | `velo-code-header-widget` / `velo-code-fold-btn` / `velo-code-fold-info` / `velo-code-wrap-btn` / 复制按钮(`velo-icon-bar` + `data-testid="code-copy-btn"` + `velo-copy-flash-ok`) / `velo-code-lang-input-wrap` / `velo-code-lang-icon` / `velo-code-lang-input` / `velo-lang-dropdown` / `velo-lang-dropdown-item`(+ `highlighted`) / `velo-lang-match` | `_editor-code.scss` |
| `CodeLineNumberWidget.ts` | `velo-code-lineno` | `_editor-code.scss` |
| `MermaidDecoration.ts` | `mermaid-node` / `mermaid-widget`(+ `is-editing`) / `mermaid-svg-area` / `mermaid-toolbar` / `mermaid-btn`(+ `mermaid-btn-toggle` / `mermaid-btn-delete`) / `mermaid-placeholder` / `mermaid-loading` / `mermaid-error` / `mermaid-error-icon` / `mermaid-error-msg` | `_mermaid.scss` |
| `MathNodeViews.ts` | `math-node`(+ `math-inline-node` / `math-block-node`) / `is-editing` / `math-inline-source` / `math-inline-display` / `math-error` / `math-empty-placeholder` | `_math.scss` |
| `TextareaEditor.ts`(math_block 编辑壳) | `edit-textarea` / `edit-preview` | `_math.scss` |
| `FootnoteNodeViews.ts` | `footnote-ref-node`(+ `footnote-orphan` / `footnote-flash`) / `footnote-definition` / `footnote-content` / `footnote-backref`(+ `footnote-backref-disabled`) | `_footnote.scss` |
| `TaskListNodeView.ts` | `task-checkbox` / `task-content` | `_editor-lists.scss` |
| `TocDecoration.ts` | `velo-toc` / `velo-toc-empty` / `velo-toc-list` / `velo-toc-item` / `velo-toc-link`(+ `velo-toc-highlight`) / `velo-toc-delete-btn` | `_editor-toc.scss` |
| `FoldDecoration.ts` | `velo-fold-toggle`(+ `data-fold-state`) / `velo-fold-placeholder` | `_editor-fold.scss` |
| `HtmlNodeView.ts` | `velo-html-block` / `velo-html-inline` | `_editor-html-blocks.scss` |
| `imageEditPlugin.ts` | `velo-image-source-preview` / `velo-image-source-edit` | `_editor-image.scss` |
| `HrNodeView.ts` | `velo-hr` | `_editor-hr.scss` |

### Decoration.inline / Decoration.node class

| TS 文件 | class 名 | SCSS 文件 |
|---------|---------|-----------|
| `linkClick.ts` | `velo-link-source-edit` | `_editor-html-blocks.scss` |
| `markSourceEdit.ts` | `data-mark-source-edit`(attribute 非 class,无 CSS 规则,仅测试 querySelector 用) | 无 |
| `findHighlight.ts` | `velo-find-match` / `velo-find-current` | `_editor-base.scss` |
| `focusMode.ts` | `velo-focus-active` | `_editor-base.scss` |
| `codeWrapPlugin`(EditorInner.vue) | `data-velo-wrap`(attribute 非 class) | `_editor-code.scss` |
| `foldDecoration`(FoldDecoration.ts) | `velo-folded` | `_editor-fold.scss` |
